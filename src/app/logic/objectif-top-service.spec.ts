import { ObjectifTopService } from './objectif-top-service';

describe('ObjectifTopService', () => {
  it('should create an instance', () => {
    expect(new ObjectifTopService()).toBeTruthy();
  });
});
